import { BarcodeModalDirective } from './barcode-modal.directive';

describe('BarcodeModalDirective', () => {
  it('should create an instance', () => {
    const directive = new BarcodeModalDirective();
    expect(directive).toBeTruthy();
  });
});
